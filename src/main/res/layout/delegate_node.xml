<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:descendantFocusability="blocksDescendants"
              android:minHeight="?attr/listPreferredItemHeight"
              android:orientation="horizontal"
              android:padding="@dimen/pad">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_listitem_primary"
            style="@style/OdsTextPrimary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawablePadding="@dimen/pad"/>

        <TextView
            android:id="@+id/text_listitem_secondary"
            style="@style/OdsTextSecondary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <ProgressBar
            android:id="@+id/view_listitem_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:visibility="gone"/>
    </LinearLayout>

    <ImageButton
        android:id="@+id/action_listitem_more"
        style="@style/OdsBorderless"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="@dimen/pad"
        android:layout_marginStart="@dimen/pad"
        android:contentDescription="@string/folder_actions"
        android:scaleType="center"
        android:src="@drawable/ic_prop"/>
</LinearLayout>
